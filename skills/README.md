# Builder Skills

Repo-local builder skills are intentionally lightweight in this phase.

Current skills:
- `phase4_enforcement`: require planner/architect/judge artifacts and evidence before acceptance.
- `runtime_proof`: require explicit runtime bootstrap and proof logging when runtime validation is part of the task.

These files are consumed by the builder execution layer as repo-local standards context.
