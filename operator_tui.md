# Operator TUI

The operator TUI is documented in:

- [docs/operator_guide.md](/Users/samuelkrystal/projects/jorb-builder/docs/operator_guide.md)

Quick start:

```bash
cd ~/projects/jorb-builder
python3 scripts/operator_tui.py
```

Snapshot mode:

```bash
python3 scripts/operator_tui.py --once
```

This file is intentionally short so the main operator workflow stays in one place.
